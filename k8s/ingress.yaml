apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nextjs-app
spec:
  rules:
  - host: nextjs-app.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: nextjs-app
            port:
              number: 80
